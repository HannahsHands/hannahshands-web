# Copy to `.env` (local) or set in Netlify → Site settings → Environment variables.

# T9 — Cloudflare Web Analytics beacon token.
# Get it free: Cloudflare dashboard → Web Analytics → add site → copy the token.
# When unset (default), no analytics script loads at all.
PUBLIC_CF_BEACON_TOKEN=
